// Compatibility re-export only.
//
// The app source is plain JavaScript, so the Vitest DOM setup lives in
// `setup.js`. This file is retained solely because the conversion cannot delete
// files; it carries no behavior of its own and is not referenced by
// `vitest.config.js`.
import "./setup.js";
